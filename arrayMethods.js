const age = [80, 55, 25, 20, 18, 26, 33]

age.map((abc, xyz) => {
    console.log(abc);
})

//Map function
// age.map((abc, xyz) => console.log(`${abc}:${xyz}`))

//Filter function
// console.log(age.filter(abc => abc < 50));

// console.log(age.slice(0,4))

// console.log(age.sort((abc, xyz) => xyz - abc))